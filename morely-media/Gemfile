source "https://rubygems.org"

# This pins your site to the same Jekyll/plugin versions GitHub Pages
# uses in production, so what you see locally matches what gets
# deployed. Run `bundle install` once, then `bundle exec jekyll serve`
# any time you want to preview the site at http://localhost:4000
gem "github-pages", group: :jekyll_plugins

gem "webrick"
